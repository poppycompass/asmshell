package main

const (
    VERSION   string = "0.1.0"
    SUPPORTED string = "i8086, x86, x64, arm-thumb(be), arm(be), arm64(be), mips(be), mips64(be), sparc(le), sparc64, [ppc|powerpc], [ppc64(le)|powerpc64(le)], [sysz|systemz|systemZ]"
    BUFSIZE uint64 = 1024
)

